const PK='adore-public-properties-v1',SK='adore-public-settings-v1';const defaults=[{id:'skyline01',name:'Skyline 01',location:'Tirana, Albania',price:75,guests:2,bedrooms:1,bathrooms:1,description:'A clean, modern apartment designed for an easy stay in Tirana.',published:true,images:['https://images.unsplash.com/photo-1522708323590-d24dbb6b0267?auto=format&fit=crop&w=1200&q=80']},{id:'skyline02',name:'Skyline 02',location:'Tirana, Albania',price:82,guests:3,bedrooms:1,bathrooms:1,description:'A bright city apartment with comfortable interiors and a central location.',published:true,images:['https://images.unsplash.com/photo-1502672260266-1c1ef2d93688?auto=format&fit=crop&w=1200&q=80']}];function props(){let x=JSON.parse(localStorage.getItem(PK)||'null');if(!x){localStorage.setItem(PK,JSON.stringify(defaults));return defaults}return x}function settings(){return JSON.parse(localStorage.getItem(SK)||'null')||{currency:'EUR'}}function sym(){return settings().currency==='GBP'?'£':settings().currency==='USD'?'$':'€'}function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}function render(g=0){let a=props().filter(p=>p.published!==false&&(!g||+p.guests>=g));grid.innerHTML=a.map(p=>`<article class="card"><img src="${esc(p.images?.[0]||'')}" alt="${esc(p.name)}"><div class="card-body"><div class="card-top"><div><h3>${esc(p.name)}</h3><div class="small">${esc(p.location)}</div></div><div class="price">${sym()}${Number(p.price).toFixed(0)} / night</div></div><div class="facts"><span>${p.guests} guests</span><span>${p.bedrooms} bedroom</span><span>${p.bathrooms} bath</span></div><p class="small">${esc(p.description||'')}</p><div class="card-actions"><button class="secondary" onclick="openProperty('${p.id}')">View</button><button class="button" onclick="openProperty('${p.id}')">Book now</button></div></div></article>`).join('')||'<p>No properties match your search.</p>';meta.textContent=`${a.length} ${a.length===1?'property':'properties'} available to browse`}function openProperty(id){let p=props().find(x=>x.id===id);if(!p)return;modalBody.innerHTML=`<div class="gallery"><img src="${esc(p.images?.[0]||'')}"><div class="gallery-side">${(p.images||[]).slice(1,3).map(i=>`<img src="${esc(i)}">`).join('')}</div></div><div class="detail"><div class="card-top"><div><h2>${esc(p.name)}</h2><div class="small">${esc(p.location)}</div></div><div class="price">${sym()}${Number(p.price).toFixed(0)} / night</div></div><div class="facts"><span>${p.guests} guests</span><span>${p.bedrooms} bedroom</span><span>${p.bathrooms} bath</span></div><p class="small">${esc(p.description||'')}</p><div class="booking"><b>Check availability</b><div class="cols" style="margin-top:12px"><label class="field">Check-in<input id="mIn" type="date" value="${checkin.value}"></label><label class="field">Check-out<input id="mOut" type="date" value="${checkout.value}"></label></div><button class="button" onclick="book()">Continue to booking</button><p class="hint">Live availability + Stripe checkout will be connected next.</p></div></div>`;modal.classList.add('show')}function closeModal(){modal.classList.remove('show')}function book(){if(!mIn.value||!mOut.value||mOut.value<=mIn.value)return alert('Choose valid dates.');alert('Next step: live availability check + Stripe payment.')}searchForm.onsubmit=e=>{e.preventDefault();render(+guests.value);stays.scrollIntoView({behavior:'smooth'})};year.textContent=new Date().getFullYear();const iso=d=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`,d=new Date(),a=new Date(d),b=new Date(d);a.setDate(d.getDate()+1);b.setDate(d.getDate()+3);checkin.value=iso(a);checkout.value=iso(b);render();